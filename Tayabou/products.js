let products = '';
const productNmae= '';
let userNmae = '';

function store(products, productNmae, userNmae) {
    name = null;
    if (name === null) {
        TouchEvent.apply('statements');
    }else if (name === '') {
        TouchEvent.apply('Please place your order');
    }else if (name.lenght === 4) {
        Touch('Alert')
    };

    return name();
}
